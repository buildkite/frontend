// @flow

import * as React from 'react';
// import { createFragmentContainer, graphql } from 'react-relay/compat';

export default class OrganizationShow extends React.Component<{}> {
  render() {
    return (
      <div>OrganizationShow</div>
    );
  }
}
