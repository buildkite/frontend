---
imports:
  awsLaunchStackButtonImage: '../../images/aws-launch-stack-button.svg'
title: AWS
---
## Install the Agent on your own AWS instance

To run the agent on your own AWS instances use whichever installer matches your instance type. For Amazon Linux use the RH/CentOS installer.

## Use our pre-built Elastic CI Stack for AWS

The [Elastic CI Stack for AWS](https://github.com/buildkite/elastic-ci-stack-for-aws) is a pre–built CloudFormtion template that gives you an autoscaling agent cluster including Docker, S3 and CloudWatch integration. [Read the documentation](https://github.com/buildkite/elastic-ci-stack-for-aws) on GitHub, and launch it using the details below:

<a href="https://console.aws.amazon.com/cloudformation/home#/stacks/new?stackName=buildkite&templateURL=https://s3.amazonaws.com/buildkite-aws-stack/aws-stack.json" target="_blank"><img src={awsLaunchStackButtonImage} alt="Launch Buildkite Elastic CI Stack" width="144" height="27"/></a>

Use the following stack parameters when launching the stack:

| **BuildkiteAgentToken** | <code>{{ props.token || 'INSERT-YOUR-AGENT-TOKEN-HERE' }}</code> |
| **BuildkiteOrgSlug** | <code>{{ props.organization.slug || 'INSERT-YOUR-ACCOUNT-SLUG-HERE' }}</code> |
| **BuildkiteApiAccessToken** | {{ props.awsTokens.map((token, index) => <code key={index}>{ token }</code>) }} {{ <a href={`/user/api-access-tokens/new?account_ids%5B%5D=${ props.organization.id }&account_scope=some&description=Elastic+CI+Stack+%28${ encodeURIComponent(props.organization.name) }%29&return_to=%2Forganizations%2F${ encodeURIComponent(props.organization.slug) }%2Fagents%23setup-aws&scopes%5B%5D=read_agents&scopes%5B%5D=read_builds&scopes%5B%5D=read_pipelines&template=elastic_ci_aws`}>{`Create an${ props.awsTokens.length ? 'other' : '' } API Access Token`}</a> }} |

<p class="rounded bg-silver p2">We recommend following AWS best practice by setting up a separate development AWS account and using role switching and consolidated billing. See the AWS tutorial on <a href="http://docs.aws.amazon.com/IAM/latest/UserGuide/tutorial_cross-account-with-roles.html">access delegation across AWS accounts</a>, and <a href="https://github.com/buildkite/elastic-ci-stack-for-aws">Elastic CI Stack repository on GitHub</a> for full documentation.</p>
