import React from 'react';

class PageLoader extends React.Component {
  render() {
    return (
      <div style={{fontSize: "30px", color: "#989898", textAlign: "center", marginTop: "30px"}}>Loading page...</div>
    );
  }
}

export default PageLoader
