version: '2'

services:
  frontend:
    build: .
    volumes:
      - "./dist:/frontend/dist"
      - "./coverage:/frontend/coverage"
    environment:
      BUILDKITE:
      CI:
      EMOJI_HOST:
      FRONTEND_HOST:
